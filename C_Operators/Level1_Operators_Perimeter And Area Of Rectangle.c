#include<stdio.h>
int main()
{
    int l,w;
    scanf("%d%d",&l,&w);
    printf("Perimeter of the rectangle = %d inches\n",2*(l+w));
    printf("Area of the rectangle = %d square inches",l*w);
}
